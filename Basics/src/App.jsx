import * as tf from "@tensorflow/tfjs";

const App = () => {
  // const shape = [2,2]
  // const tensor1 = tf.tensor([1, 2, 3, 8], shape);
  // const tensor2d = tf.tensor2d([[1, 2], [3, 4]]);
  // const sum = tensor1.add(tf.scalar(2));
  // const sub = tensor1.sub(tensor2d)
  // const div = tensor1.div(tensor2d)

  // const product = tensor1.mul(tensor2);
  // const matMul = tf.matMul(tensor2, tensor2);
  // const reshapedTensor2 = tensor2d.reshape([1,4])
  // const slicedTensor = tensor2d.slice([0,0], [1,2])
  // const dicedTensor = tensor2d.gather([0], 0)
  // slicedTensor.print()   
  // dicedTensor.print()
  // reshapedTensor2.print()
  // tensor2.print()
  // sum.print()
  // product.print()
  // matMul.print()
  return <div>App</div>;
};

export default App;
